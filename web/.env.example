# Base URL of the NestJS API. For local web dev, localhost works (browser is on the same machine).
# In production (Netlify) this is "/api/v1" — same origin, see .env.production.
VITE_API_URL=http://localhost:3000/api/v1
